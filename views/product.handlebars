{{!-- This template will display data for a specific product --}}


<div class="text-center">
  <h2>{{product.name}} - ${{product.price}}</h2>
  {{!-- still need to make imgaes work --}}
  <img src="../{{product.image}}">
  <p>{{product.description}}</p>
</div>

{{#each review}}
<h4>{{date_created}}</h4>
<p>{{content}}</p>
{{!-- display user by id? --}}
{{/each}}
{{!-- <div>
  
  <p>
    <span for="img" aria-label="Product_name">💲</span>
    <span class="Poduct-description">{{format_amount needed_funding}}</span>
    
  </p>
  <p>
    <button class="btn btn-primary">Add to cart</button>
  </p>
</div> --}}
