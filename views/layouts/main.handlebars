<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech EStore</title>
  <link rel="stylesheet" href="/public/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

<body>
  <div>
    <header>
      <h1 class="py-4 is-size-1 has-text-weight-bold has-text-left has-background-link-dark">
        <a class="has-text-black" href="/">Welcome to the Tech EStore</a>
      </h1>
      <nav class="py-2 is-size-3 has-text-link-dark has-text-right has-background-link-light">
        {{!-- add a homepage, product, category, and cart nav --}}
        | <a href="/">Homepage</a> |
        <a href="/profile">Profile</a> |
        <a href="/cart">Cart</a> |
        {{#if logged_in}}
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a> |
        {{/if}}
      </nav>
    </header>
    <main class="section mt-5 ml-5 mr-5 mb-5">
      <div class="container">
        {{{ body }}}
      </div>
    </main>
    <footer>
      
    </footer>
  </div>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
